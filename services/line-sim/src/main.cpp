#include <cstdio>

#include "scheduler.hpp"

int main() {
    // Producer wiring lands in Phase 4; until then the binary proves the toolchain.
    std::printf("line-sim %s\n", line_sim::kVersion);
    return 0;
}
